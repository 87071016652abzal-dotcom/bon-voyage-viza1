---
export const prerender = false;

if (Astro.request.method === "POST") {
  try {
    const data = await Astro.request.formData();
    const name = data.get("name") || "Не указано";
    const phone = data.get("phone") || "Не указано";
    const country = data.get("country") || "Не указано";

    const token = import.meta.env.TELEGRAM_BOT_TOKEN || import.meta.env.TELEGRAM_TOKEN;
    const chatId = import.meta.env.TELEGRAM_CHAT_ID;

    if (!token || !chatId) {
      console.error("ОШИБКА: Ключи Telegram не найдены в Environment Variables Vercel");
    }

    const text = `🚀 НОВАЯ ЗАЯВКА!\n👤 Имя: ${name}\n📞 Тел: ${phone}\n🌍 Куда: ${country}`;

    const response = await fetch(`https://api.telegram.org/bot${token}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: chatId, text: text }),
    });

    if (response.ok) {
      return Astro.redirect("/?done=true");
    } else {
      const errorData = await response.json();
      console.error("Ошибка от Telegram API:", errorData);
    }
  } catch (e) {
    console.error("Критическая ошибка на сервере:", e);
  }
}

const done = Astro.url.searchParams.get("done");
---

<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>BON VOYAGE | Визовый центр в Алматы</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;600;700&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet" />
  <style>
    :root {
      --gold: #c9a96e;
      --gold-light: #e8d5b0;
      --gold-dark: #a07c45;
      --black: #080808;
      --dark: #0e0e0e;
      --card: #131313;
      --border: rgba(201, 169, 110, 0.15);
      --text-muted: rgba(255,255,255,0.45);
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }
    html { scroll-behavior: smooth; }

    body {
      background: var(--black);
      color: #fff;
      font-family: 'DM Sans', sans-serif;
      overflow-x: hidden;
    }

    /* Scrollbar */
    ::-webkit-scrollbar { width: 4px; }
    ::-webkit-scrollbar-track { background: var(--black); }
    ::-webkit-scrollbar-thumb { background: var(--gold-dark); border-radius: 2px; }

    /* ─── HEADER ─── */
    header {
      position: fixed; top: 0; width: 100%; z-index: 100;
      display: flex; justify-content: space-between; align-items: center;
      padding: 22px 6%;
      background: rgba(8, 8, 8, 0.9);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--border);
      transition: padding 0.3s;
    }

    .logo {
      font-family: 'Cormorant Garamond', serif;
      font-size: 24px;
      font-weight: 700;
      letter-spacing: 4px;
      text-transform: uppercase;
      cursor: pointer;
      color: #fff;
    }
    .logo span { color: var(--gold); }

    nav { display: flex; align-items: center; gap: 36px; }
    nav a {
      color: rgba(255,255,255,0.65);
      text-decoration: none;
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 2.5px;
      text-transform: uppercase;
      transition: color 0.3s;
    }
    nav a:hover { color: var(--gold); }

    .nav-cta {
      background: var(--gold) !important;
      color: var(--black) !important;
      padding: 10px 24px;
      border-radius: 3px;
      font-weight: 600 !important;
    }
    .nav-cta:hover { background: var(--gold-light) !important; }

    .burger {
      display: none;
      flex-direction: column;
      gap: 5px;
      cursor: pointer;
      padding: 5px;
    }
    .burger span {
      display: block;
      width: 24px; height: 2px;
      background: #fff;
      border-radius: 2px;
      transition: 0.3s;
    }

    /* ─── HERO ─── */
    .hero {
      position: relative;
      min-height: 100vh;
      display: flex;
      align-items: center;
      padding: 120px 6% 80px;
      overflow: hidden;
    }

    .hero-bg {
      position: absolute; inset: 0;
      background: url('https://images.unsplash.com/photo-1436491865332-7a61a109cc05?auto=format&fit=crop&q=80&w=2074') center/cover no-repeat;
      filter: brightness(0.25) saturate(0.6);
      z-index: 0;
    }

    .hero-overlay {
      position: absolute; inset: 0;
      background: linear-gradient(135deg, rgba(8,8,8,0.7) 0%, rgba(8,8,8,0.2) 60%, rgba(201,169,110,0.05) 100%);
      z-index: 1;
    }

    /* decorative line */
    .hero-line {
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 3px;
      background: linear-gradient(90deg, transparent, var(--gold), transparent);
      z-index: 2;
    }

    .hero-inner {
      position: relative; z-index: 2;
      width: 100%;
      display: grid;
      grid-template-columns: 1.1fr 0.9fr;
      gap: 80px;
      align-items: center;
    }

    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      background: rgba(201,169,110,0.1);
      border: 1px solid var(--border);
      border-radius: 30px;
      padding: 8px 18px;
      margin-bottom: 28px;
    }
    .hero-badge-dot {
      width: 6px; height: 6px;
      border-radius: 50%;
      background: var(--gold);
      animation: pulse 2s infinite;
    }
    @keyframes pulse {
      0%, 100% { opacity: 1; transform: scale(1); }
      50% { opacity: 0.5; transform: scale(1.4); }
    }
    .hero-badge span {
      font-size: 11px;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: var(--gold);
      font-weight: 500;
    }

    .hero-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(42px, 5.5vw, 82px);
      line-height: 1.0;
      font-weight: 600;
      margin-bottom: 28px;
    }
    .hero-title .accent { color: var(--gold); font-style: italic; }

    .hero-desc {
      font-size: 15px;
      line-height: 1.8;
      color: rgba(255,255,255,0.55);
      max-width: 460px;
      margin-bottom: 44px;
      font-weight: 300;
    }

    .hero-stats {
      display: flex;
      gap: 40px;
      border-top: 1px solid var(--border);
      padding-top: 32px;
    }
    .stat-num {
      font-family: 'Cormorant Garamond', serif;
      font-size: 36px;
      font-weight: 700;
      color: var(--gold);
      line-height: 1;
    }
    .stat-label {
      font-size: 11px;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 1.5px;
      margin-top: 4px;
    }

    /* ─── FORM CARD ─── */
    .form-card {
      background: rgba(13, 13, 13, 0.95);
      border: 1px solid var(--border);
      border-radius: 4px;
      padding: 44px 40px;
      position: relative;
      overflow: hidden;
    }
    .form-card::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 2px;
      background: linear-gradient(90deg, transparent, var(--gold), transparent);
    }

    .form-label {
      display: block;
      font-size: 10px;
      letter-spacing: 2.5px;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 8px;
      font-weight: 500;
    }

    .form-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: 28px;
      font-weight: 600;
      margin-bottom: 8px;
    }
    .form-subtitle {
      font-size: 13px;
      color: var(--text-muted);
      margin-bottom: 32px;
      padding-bottom: 28px;
      border-bottom: 1px solid var(--border);
    }

    .field-group { margin-bottom: 16px; }
    .field-label {
      font-size: 10px;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      color: var(--text-muted);
      display: block;
      margin-bottom: 8px;
    }

    input, select {
      width: 100%;
      background: rgba(255,255,255,0.04);
      border: 1px solid rgba(255,255,255,0.1);
      color: #fff;
      padding: 14px 16px;
      border-radius: 3px;
      font-family: 'DM Sans', sans-serif;
      font-size: 14px;
      outline: none;
      transition: border-color 0.3s, background 0.3s;
      -webkit-appearance: none;
    }
    input:focus, select:focus {
      border-color: var(--gold);
      background: rgba(201,169,110,0.05);
    }
    input::placeholder { color: rgba(255,255,255,0.25); }
    select option { background: #1a1a1a; color: #fff; }

    .select-wrapper { position: relative; }
    .select-wrapper::after {
      content: '▾';
      position: absolute; right: 16px; top: 50%; transform: translateY(-50%);
      color: var(--gold); pointer-events: none; font-size: 12px;
    }

    .btn-submit {
      width: 100%;
      background: linear-gradient(135deg, var(--gold-dark), var(--gold));
      color: var(--black);
      padding: 16px;
      font-weight: 700;
      font-size: 12px;
      letter-spacing: 3px;
      text-transform: uppercase;
      border: none;
      border-radius: 3px;
      cursor: pointer;
      transition: all 0.3s;
      margin-top: 8px;
      font-family: 'DM Sans', sans-serif;
    }
    .btn-submit:hover {
      background: linear-gradient(135deg, var(--gold), var(--gold-light));
      transform: translateY(-1px);
      box-shadow: 0 8px 30px rgba(201,169,110,0.25);
    }

    .form-note {
      font-size: 11px;
      color: var(--text-muted);
      text-align: center;
      margin-top: 14px;
    }

    /* ─── SUCCESS ─── */
    .success-state {
      text-align: center;
      padding: 20px 0;
    }
    .success-icon {
      width: 64px; height: 64px;
      border-radius: 50%;
      border: 2px solid var(--gold);
      display: flex; align-items: center; justify-content: center;
      margin: 0 auto 24px;
      font-size: 24px;
    }
    .success-state h2 {
      font-family: 'Cormorant Garamond', serif;
      font-size: 32px;
      color: var(--gold);
      margin-bottom: 10px;
    }
    .success-state p { color: var(--text-muted); font-size: 14px; line-height: 1.7; }
    .back-link {
      display: inline-block;
      margin-top: 24px;
      color: var(--gold);
      text-decoration: none;
      font-size: 12px;
      letter-spacing: 2px;
      text-transform: uppercase;
      border-bottom: 1px solid var(--border);
      padding-bottom: 2px;
    }

    /* ─── SECTION BASE ─── */
    .section {
      padding: 110px 6%;
      border-top: 1px solid rgba(255,255,255,0.04);
    }

    .section-eyebrow {
      font-size: 11px;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 16px;
      font-weight: 500;
    }

    .section-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(36px, 4vw, 58px);
      font-weight: 600;
      line-height: 1.1;
      margin-bottom: 20px;
    }
    .section-title span { color: var(--gold); font-style: italic; }

    .section-desc {
      font-size: 15px;
      color: var(--text-muted);
      max-width: 520px;
      line-height: 1.8;
      font-weight: 300;
    }

    /* ─── SERVICES ─── */
    #services { background: var(--dark); }

    .services-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      margin-bottom: 64px;
      gap: 30px;
      flex-wrap: wrap;
    }

    .services-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1px;
      background: var(--border);
      border: 1px solid var(--border);
      border-radius: 4px;
      overflow: hidden;
    }

    .service-card {
      background: var(--dark);
      padding: 50px 40px;
      position: relative;
      transition: background 0.4s;
      overflow: hidden;
    }
    .service-card::before {
      content: '';
      position: absolute;
      bottom: 0; left: 0; right: 0;
      height: 2px;
      background: linear-gradient(90deg, transparent, var(--gold), transparent);
      opacity: 0;
      transition: opacity 0.4s;
    }
    .service-card:hover { background: rgba(201,169,110,0.04); }
    .service-card:hover::before { opacity: 1; }

    .service-num {
      font-family: 'Cormorant Garamond', serif;
      font-size: 60px;
      font-weight: 300;
      color: rgba(201,169,110,0.12);
      line-height: 1;
      margin-bottom: 24px;
    }

    .service-card h3 {
      font-family: 'Cormorant Garamond', serif;
      font-size: 28px;
      font-weight: 600;
      color: var(--gold);
      margin-bottom: 14px;
    }

    .service-card p {
      font-size: 14px;
      line-height: 1.8;
      color: var(--text-muted);
      font-weight: 300;
    }

    .service-detail {
      margin-top: 24px;
      padding-top: 24px;
      border-top: 1px solid var(--border);
      font-size: 12px;
      letter-spacing: 1px;
      color: rgba(201,169,110,0.6);
      text-transform: uppercase;
    }

    /* ─── WHY US ─── */
    #why { background: var(--black); }
    .why-inner {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 80px;
      align-items: center;
    }
    .why-list { display: flex; flex-direction: column; gap: 0; }
    .why-item {
      display: flex;
      gap: 24px;
      padding: 28px 0;
      border-bottom: 1px solid var(--border);
      align-items: flex-start;
    }
    .why-item:first-child { border-top: 1px solid var(--border); }
    .why-icon {
      width: 44px; height: 44px; min-width: 44px;
      border: 1px solid var(--border);
      border-radius: 3px;
      display: flex; align-items: center; justify-content: center;
      font-size: 18px;
      flex-shrink: 0;
    }
    .why-item h4 {
      font-size: 15px;
      font-weight: 600;
      margin-bottom: 6px;
    }
    .why-item p {
      font-size: 13px;
      color: var(--text-muted);
      line-height: 1.7;
      font-weight: 300;
    }
    .why-image {
      position: relative;
      border-radius: 4px;
      overflow: hidden;
      aspect-ratio: 4/5;
    }
    .why-image img {
      width: 100%; height: 100%;
      object-fit: cover;
      filter: brightness(0.7) saturate(0.6);
    }
    .why-image-badge {
      position: absolute;
      bottom: 28px; left: 28px;
      background: rgba(8,8,8,0.92);
      border: 1px solid var(--border);
      border-radius: 3px;
      padding: 16px 24px;
    }
    .why-image-badge .big {
      font-family: 'Cormorant Garamond', serif;
      font-size: 42px;
      font-weight: 700;
      color: var(--gold);
      line-height: 1;
    }
    .why-image-badge .small {
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 2px;
      color: var(--text-muted);
      margin-top: 4px;
    }

    /* ─── CONTACTS ─── */
    #contacts { background: var(--dark); }
    .contacts-inner {
      display: grid;
      grid-template-columns: 1fr 1.8fr;
      gap: 60px;
      align-items: start;
    }

    .contact-item {
      padding: 28px 0;
      border-bottom: 1px solid var(--border);
    }
    .contact-item:first-child { border-top: 1px solid var(--border); }
    .contact-item-label {
      font-size: 10px;
      letter-spacing: 2.5px;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 8px;
      font-weight: 500;
    }
    .contact-item-value {
      font-size: 16px;
      font-weight: 400;
      line-height: 1.5;
    }
    .contact-item-sub {
      font-size: 12px;
      color: var(--text-muted);
      margin-top: 3px;
    }

    .map-wrap {
      border: 1px solid var(--border);
      border-radius: 4px;
      overflow: hidden;
      position: relative;
    }
    .map-wrap::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 2px;
      background: linear-gradient(90deg, transparent, var(--gold), transparent);
      z-index: 1;
    }
    .map-wrap iframe {
      display: block;
      width: 100%;
      height: 460px;
      border: none;
      filter: grayscale(0.5) brightness(0.8);
    }

    /* ─── FOOTER ─── */
    footer {
      padding: 40px 6%;
      border-top: 1px solid var(--border);
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 16px;
    }
    .footer-logo {
      font-family: 'Cormorant Garamond', serif;
      font-size: 18px;
      font-weight: 700;
      letter-spacing: 3px;
    }
    .footer-logo span { color: var(--gold); }
    footer p {
      font-size: 12px;
      color: var(--text-muted);
      letter-spacing: 1px;
    }

    /* ─── MOBILE NAV ─── */
    .mobile-nav {
      display: none;
      position: fixed; top: 0; left: 0; right: 0; bottom: 0;
      background: rgba(8,8,8,0.98);
      z-index: 99;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 40px;
    }
    .mobile-nav.open { display: flex; }
    .mobile-nav a {
      color: #fff;
      text-decoration: none;
      font-family: 'Cormorant Garamond', serif;
      font-size: 36px;
      font-weight: 600;
      transition: color 0.3s;
    }
    .mobile-nav a:hover { color: var(--gold); }
    .mobile-close {
      position: absolute; top: 24px; right: 6%;
      font-size: 28px; cursor: pointer; color: #fff;
      background: none; border: none;
    }

    /* ─── RESPONSIVE ─── */
    @media (max-width: 1024px) {
      .hero-inner { grid-template-columns: 1fr; gap: 50px; }
      .hero-text { max-width: 100%; }
      .hero-desc { max-width: 100%; }
      .services-grid { grid-template-columns: 1fr; }
      .why-inner { grid-template-columns: 1fr; gap: 50px; }
      .why-image { aspect-ratio: 16/9; }
      .contacts-inner { grid-template-columns: 1fr; }
    }

    @media (max-width: 768px) {
      header { padding: 18px 5%; }
      nav { display: none; }
      .burger { display: flex; }

      .hero { padding: 100px 5% 60px; }
      .hero-title { font-size: 38px; }
      .hero-stats { gap: 24px; flex-wrap: wrap; }
      .stat-num { font-size: 28px; }

      .form-card { padding: 32px 24px; }

      .section { padding: 70px 5%; }
      .services-header { flex-direction: column; align-items: flex-start; }

      .service-card { padding: 36px 28px; }
      .service-num { font-size: 44px; }

      footer { padding: 30px 5%; flex-direction: column; text-align: center; }

      .contacts-inner { gap: 40px; }
      .map-wrap iframe { height: 320px; }
    }

    @media (max-width: 480px) {
      .hero-title { font-size: 32px; }
      .hero-badge { display: none; }
      .hero-stats { gap: 20px; }
    }

    /* ─── ANIMATIONS ─── */
    .fade-up {
      opacity: 0;
      transform: translateY(30px);
      transition: opacity 0.7s ease, transform 0.7s ease;
    }
    .fade-up.visible {
      opacity: 1;
      transform: translateY(0);
    }
    .fade-up:nth-child(2) { transition-delay: 0.1s; }
    .fade-up:nth-child(3) { transition-delay: 0.2s; }
    .fade-up:nth-child(4) { transition-delay: 0.3s; }
  </style>
</head>
<body>

  <!-- MOBILE NAV -->
  <div class="mobile-nav" id="mobileNav">
    <button class="mobile-close" onclick="closeMobileNav()">✕</button>
    <a href="#home" onclick="closeMobileNav()">Главная</a>
    <a href="#services" onclick="closeMobileNav()">Услуги</a>
    <a href="#why" onclick="closeMobileNav()">О нас</a>
    <a href="#contacts" onclick="closeMobileNav()">Контакты</a>
  </div>

  <!-- HEADER -->
  <header>
    <div class="logo" onclick="window.scrollTo({top:0,behavior:'smooth'})">BON <span>VOYAGE</span></div>
    <nav>
      <a href="#home">Главная</a>
      <a href="#services">Услуги</a>
      <a href="#why">О нас</a>
      <a href="#contacts">Контакты</a>
      <a href="#home" class="nav-cta">Заявка</a>
    </nav>
    <div class="burger" onclick="openMobileNav()">
      <span></span><span></span><span></span>
    </div>
  </header>

  <!-- HERO -->
  <section class="hero" id="home">
    <div class="hero-bg"></div>
    <div class="hero-overlay"></div>
    <div class="hero-line"></div>

    <div class="hero-inner">
      <div class="hero-text">
        <div class="hero-badge">
          <div class="hero-badge-dot"></div>
          <span>Алматы — Желтоксан 111 А</span>
        </div>
        <h1 class="hero-title">
          Ваш паспорт<br>
          в <span class="accent">большой мир</span>
        </h1>
        <p class="hero-desc">
          Профессиональная помощь в получении виз в Европу, США и Азию. Мы берём на себя всю бумажную работу — вам остаётся только паковать чемодан.
        </p>
        <div class="hero-stats">
          <div>
            <div class="stat-num">500+</div>
            <div class="stat-label">Виз получено</div>
          </div>
          <div>
            <div class="stat-num">5+</div>
            <div class="stat-label">Лет опыта</div>
          </div>
          <div>
            <div class="stat-num">98%</div>
            <div class="stat-label">Одобрений</div>
          </div>
        </div>
      </div>

      <!-- FORM -->
      <div class="form-card">
        {done ? (
          <div class="success-state">
            <div class="success-icon">✓</div>
            <h2>Спасибо!</h2>
            <p>Ваша заявка принята.<br />Мы свяжемся с вами в ближайшее время.</p>
            <a href="/" class="back-link">← Вернуться</a>
          </div>
        ) : (
          <>
            <div class="form-label">Бесплатная консультация</div>
            <div class="form-title">Оставить заявку</div>
            <div class="form-subtitle">Заполните форму и мы перезвоним вам в течение часа</div>

            <form method="POST" autocomplete="off">
              <div class="field-group">
                <label class="field-label">Ваше имя</label>
                <input type="text" name="name" placeholder="Введите имя" autocomplete="one-time-code" required />
              </div>
              <div class="field-group">
                <label class="field-label">Номер телефона</label>
                <input type="text" name="phone" placeholder="+7 ___ ___ __ __" autocomplete="one-time-code" required />
              </div>
              <div class="field-group">
                <label class="field-label">Направление</label>
                <div class="select-wrapper">
                  <select name="country" required>
                    <option value="" disabled selected>Выберите страну / визу</option>
                    <option>ШЕНГЕН</option>
                    <option>США</option>
                    <option>КОРЕЯ</option>
                    <option>ЯПОНИЯ</option>
                  </select>
                </div>
              </div>
              <button type="submit" class="btn-submit">Отправить заявку</button>
              <p class="form-note">Бесплатно · Без обязательств · Конфиденциально</p>
            </form>
          </>
        )}
      </div>
    </div>
  </section>

  <!-- SERVICES -->
  <section class="section" id="services">
    <div class="services-header">
      <div>
        <div class="section-eyebrow">Наши услуги</div>
        <h2 class="section-title">Визы в любую<br><span>точку мира</span></h2>
      </div>
      <p class="section-desc">
        Занимаемся оформлением виз под ключ. Анализируем ваш профиль, собираем документы и сопровождаем до момента получения.
      </p>
    </div>

    <div class="services-grid">
      <div class="service-card fade-up">
        <div class="service-num">01</div>
        <h3>Шенген</h3>
        <p>Помогаем с записью в консульство и формируем полный пакет документов. Германия, Франция, Испания, Италия и другие страны.</p>
        <div class="service-detail">26 стран · от 14 дней</div>
      </div>
      <div class="service-card fade-up">
        <div class="service-num">02</div>
        <h3>США</h3>
        <p>Профессиональное заполнение анкеты DS-160, детальная подготовка к интервью в посольстве. Туристические и рабочие визы.</p>
        <div class="service-detail">B1/B2 · F1 · L1</div>
      </div>
      <div class="service-card fade-up">
        <div class="service-num">03</div>
        <h3>Азия</h3>
        <p>Оформление виз в Японию и Корею с минимальным пакетом документов. Быстро и с гарантией результата.</p>
        <div class="service-detail">Япония · Корея · от 7 дней</div>
      </div>
    </div>
  </section>

  <!-- WHY US -->
  <section class="section" id="why">
    <div class="why-inner">
      <div class="why-image">
        <img src="https://images.unsplash.com/photo-1488085061387-422e29b40080?auto=format&fit=crop&q=80&w=1000" alt="Путешествия" />
        <div class="why-image-badge">
          <div class="big">98%</div>
          <div class="small">Одобрения виз</div>
        </div>
      </div>

      <div>
        <div class="section-eyebrow">Почему мы</div>
        <h2 class="section-title" style="margin-bottom:14px;">Ваш надёжный<br><span>визовый партнёр</span></h2>
        <p class="section-desc" style="margin-bottom:40px;">
          За 5 лет работы мы помогли сотням клиентов получить визу и отправиться в путешествие мечты.
        </p>

        <div class="why-list">
          <div class="why-item">
            <div class="why-icon">✈</div>
            <div>
              <h4>Полное сопровождение</h4>
              <p>Берём на себя весь процесс — от записи на подачу до получения готовой визы.</p>
            </div>
          </div>
          <div class="why-item">
            <div class="why-icon">📋</div>
            <div>
              <h4>Точный список документов</h4>
              <p>Составляем персональный список под ваш профиль. Никаких лишних справок.</p>
            </div>
          </div>
          <div class="why-item">
            <div class="why-icon">⚡</div>
            <div>
              <h4>Быстрые сроки</h4>
              <p>Работаем оперативно. Срочное оформление для тех, кто не может ждать.</p>
            </div>
          </div>
          <div class="why-item">
            <div class="why-icon">🛡</div>
            <div>
              <h4>Гарантия качества</h4>
              <p>Если в документах ошибка по нашей вине — переделаем бесплатно.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CONTACTS -->
  <section class="section" id="contacts">
    <div class="section-eyebrow">Контакты</div>
    <h2 class="section-title" style="margin-bottom:50px;">Наш <span>офис</span></h2>

    <div class="contacts-inner">
      <div>
        <div class="contact-item">
          <div class="contact-item-label">Адрес</div>
          <div class="contact-item-value">г. Алматы, ул. Желтоксан, 111 А</div>
          <div class="contact-item-sub">Офис 1, угол ул. Толе Би</div>
        </div>
        <div class="contact-item">
          <div class="contact-item-label">Телефон</div>
          <div class="contact-item-value">+7 778 004 44 13</div>
          <div class="contact-item-sub">Звонки и WhatsApp</div>
        </div>
        <div class="contact-item">
          <div class="contact-item-label">График работы</div>
          <div class="contact-item-value">Пн–Пт: 09:00 — 18:00</div>
          <div class="contact-item-sub">Сб: по записи</div>
        </div>
      </div>

      <div class="map-wrap">
        <iframe
          src="https://yandex.ru/map-widget/v1/?ll=76.939223%2C43.254714&z=16&pt=76.939223%2C43.254714,pm2blm"
          allowfullscreen="true">
        </iframe>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <div class="footer-logo">BON <span>VOYAGE</span></div>
    <p>© 2026 Bon Voyage · Алматы, Желтоксан 111 А</p>
    <p>+7 778 004 44 13</p>
  </footer>

  <script>
    function openMobileNav() {
      document.getElementById('mobileNav').classList.add('open');
      document.body.style.overflow = 'hidden';
    }
    function closeMobileNav() {
      document.getElementById('mobileNav').classList.remove('open');
      document.body.style.overflow = '';
    }

    // Fade in on scroll
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); } });
    }, { threshold: 0.15 });
    document.querySelectorAll('.fade-up').forEach(el => observer.observe(el));
  </script>
</body>
</html>
